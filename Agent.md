# Agent.md — 代码编写规则

本文件为在本仓库中工作的 AI Agent / 开发者约定统一的编码规范，请在提交代码前逐条对照。

## 1. 通用原则

- 代码风格保持一致：优先遵循项目已有代码的写法，不要引入与现有风格冲突的新风格。
- 一次提交只做一件事，提交信息使用简洁祈使句，例如 `fix: 修复首页图片懒加载失败`。
- 不留调试残留：提交前删除 `console.log`、注释掉的死代码和 TODO 占位符（确需保留的 TODO 需注明原因与责任人）。
- 不引入未使用的依赖、变量和导入。

## 2. 前端代码

- 组件保持单一职责，单个文件建议不超过 300 行，超出时拆分。
- 样式类名语义化，避免 `div1`、`box2` 这类无意义命名。
- 图片等资源必须有合理尺寸与懒加载策略，不直接引用未经压缩的大图。
- 所有交互元素需考虑可访问性：`alt`、`aria-label`、键盘可操作。
- 文案与数据分离：展示文案不要硬编码在逻辑深处，便于后续多语言扩展。

## 3. 命名规范

- 变量 / 函数：`camelCase`；组件 / 类：`PascalCase`；常量：`UPPER_SNAKE_CASE`；文件名与仓库现有约定保持一致。
- 布尔变量用 `is / has / can / should` 前缀，函数名用动词开头。

## 4. 安全与隐私

- 绝不提交密钥、Token、`.env` 文件到仓库；敏感配置统一走环境变量。
- 不信任外部输入：任何用户输入或接口返回数据在渲染前必须校验 / 转义，防止 XSS。
- 第三方依赖引入前先确认许可证兼容且项目仍被维护。

## 5. 提交与审查

- 新功能在独立分支上开发，通过 Pull Request 合入主分支。
- PR 必须包含：改动说明、自测结果、影响范围；关联的 issue 需一并引用。
- 合入前至少一人 review，CI 检查必须通过。

## 6. 给 AI Agent 的附加要求

- 修改文件前先读取现有内容，编辑范围严格限于任务所需，不做顺手重构。
- 生成文件后需实际验证可运行（构建 / 语法检查 / 打开页面），不能只写不测。
- 遇到歧义需求时先提问澄清，不要自行猜测扩大改动范围。
